import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['anjanette-unabetting-exactly.ngrok-free.dev'],
} as any;

export default nextConfig;
